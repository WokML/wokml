module Main
import Base
import String

-- Golden hash corpus: pin sz_hash of a multi-byte UTF-8 string.
-- Regenerate ONLY on a deliberate StringZilla version bump.
-- Input: "héllo" (5 codepoints, 6 UTF-8 bytes: h 0xC3 0xA9 l l o).

main : U64
main = hash "héllo"
