module Main
import Base
effect Counter = { tick : () -> () , read : U64 }
demo : U64
demo = with Counter {
    once tick u k -> k ()
    once read k   -> k 7
  } (let a = Counter.tick () in Counter.read)
main : U64
main = demo
