module Main
import Base

effect Exn = { throw : String -> Never }

risky : Bool -> U64 with Exn
risky b = if b then Exn.throw "boom" else 7

main : U64
main =
  with { once Exn.throw msg k -> 0
         ; return v -> v }
  risky True
