module Main
import Base

effect State s = { get : s, set : s -> () }

prog : () -> U64 with State U64
prog u = State.get

main : U64
main =
  with { once State.get _    -> 0
         ; once State.set s _ -> 0
         ; return v             -> v }
  prog ()
