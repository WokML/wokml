module Main
import Base
import Control

prog : () -> U64 with State U64
prog u = let a = State.get in let b = State.set (a + 5) in State.get

main : U64
main = let (result, _) = (handle state 100 in prog ()) in result
