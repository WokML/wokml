module Main
import Std.Base
import Std.Control

prog : State U64 -> U64 with State U64
prog s = let a = State.get in let b = State.set (a + 5) in State.get

main : U64
main = let (result, _) = state 100 prog in result
