module Main
import Base
import Bytes

-- round-trip: length (fromList (toList b)) == length b
-- fromList [5, 6, 7] -> toList -> fromList -> length 3
main : U64
main = length (fromList (toList (fromList [5, 6, 7])))
