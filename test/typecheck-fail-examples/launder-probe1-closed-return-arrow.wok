module Main
import Base
import Control
-- A curried function returns a closed arrow (no `with eff e`) whose body
-- runs the trapped continuation. Expect UndischargedEffect.
foo : U64 -> Suspension U64 U64 U64 (row e) -> U64
foo n = \ g -> run g 0
main : U64
main = 0
