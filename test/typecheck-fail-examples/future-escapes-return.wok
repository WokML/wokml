module Main
import Base
import Control
-- Carrier rule: a Suspension-typed parameter is a second-class handle.
-- Returning it bare from `leak` must be rejected with CarrierEscape.
leak : Suspension U64 U64 U64 (row e) -> Suspension U64 U64 U64 (row e)
leak s = s
main : U64
main = 0
