module Main
import Base

effect IO = { read : String -> String, write : String -> () }

-- A handler may have at most one `return` arm. A second one is rejected
-- rather than silently ignored.
bad comp =
  with { IO.read p -> p
       ; IO.write m -> ()
       ; v -> v
       ; w -> w }
  comp ()
