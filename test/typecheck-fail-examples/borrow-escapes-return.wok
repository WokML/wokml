module Main
import Std.Base
import Std.Borrow

-- Borrow is second-class: returning it out of the scope it was received in
-- is a CarrierEscape, exactly like Suspension/Step/ContCell, even though
-- Borrow is NOT affine (see borrow-read-twice.wok).
g : Borrow -> Borrow
g b = b
