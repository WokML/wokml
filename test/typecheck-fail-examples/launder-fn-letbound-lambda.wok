module Main
import Base
import Control
-- A let-bound lambda point-freely returns the trapped-effect closure; the
-- outer sig has no channel for e. Expect UndischargedEffect.
helper : Suspension U64 U64 U64 (row e) -> U64
helper = let k = \ g -> run g 0 in k
main : U64
main = 0
