module Main
import Base
import Control
-- A sig-less nested local `go` runs the trapped continuation inside a
-- lambda body; the outer sig has no channel for e. Expect UndischargedEffect.
helper : Suspension U64 U64 U64 (row e) -> U64
helper = \ g -> let go h = run h 0 in go g
main : U64
main = 0
