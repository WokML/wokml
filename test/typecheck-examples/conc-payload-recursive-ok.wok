module Main
import Base
import Control

-- A recursive data type WITHOUT functions is a legal carrier payload: the
-- constructor-field walk must terminate (visited set) and accept it.

data Tree = Leaf U64 | Node Tree Tree

ok : Chan Tree -> U64
ok c = 0

main : U64
main = 0
