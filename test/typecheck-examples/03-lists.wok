module Main
import Base

myLength : [a] -> U64
myLength xs = case xs of
  []      -> 0
  _ :: ys -> 1 + myLength ys

-- Expression-level cons isn't a grammar primitive; we rebuild via (++).
myMap : (a -> b) -> [a] -> [b]
myMap f xs = case xs of
  []      -> []
  y :: ys -> [f y] ++ myMap f ys
