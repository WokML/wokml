module Main
import Base
-- A module that does NOT import Control may freely name a top-level
-- function `value`: the reservation is gated on `value` being in scope via
-- import (i.e. on importing Control). This must type-check.
value : U64 -> U64
value x = x
main : U64
main = value 7
