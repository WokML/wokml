module Main
import Base
import Bytes

-- __ffi_demo_adopt 10 yields bytes 0..9 (valid UTF-8 control chars).
-- fromBytes returns Some; length of the raw Bytes buffer is 10.
main : U64
main =
  case fromBytes (__ffi_demo_adopt 10) of
    Some _ -> length (__ffi_demo_adopt 10)
    None   -> 0
