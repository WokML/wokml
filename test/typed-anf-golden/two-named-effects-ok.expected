prog =
  let t.5 : TcHandler "State" U64 U64 (U64, U64) = state(0)
  install t.5 in
  let t.4 : TcHandler "Reader" U64 U64 U64 = reader(10)
  install t.4 in
  let t.2 : U64 = r.Reader.ask()
  let t.3 : () = s.State.set(t.2)
  let u : () = t.3
  let t.1 : U64 = s.State.get()
  t.1

main =
  prog
