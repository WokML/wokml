module Main
import Std.Base
import Std.String

-- Out-of-bounds decodeCharAt: exercises the offset >= byteLength guard in
-- decodeCharAt. "abc" has byteLength 3; offset 5 is past the end, so both
-- the reference and RC interpreters must raise a PrimError.
main : Char
main = decodeCharAt "abc" 5
