id = \x -> x
const = \x y -> x

addOne x =
  let y = x + 1
  in y

multi x =
  let
    y = x + 1
    z = y * 2
  in z

bool b = case b of
  True -> 1
  False -> 0

select x = if x then "yes" else "no"

multiBranch x =
  case x of
    0 -> "zero"
    1 -> "one"
    _ -> "other"
