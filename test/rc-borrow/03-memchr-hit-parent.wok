module Main
import Std.Base
import Std.Borrow

-- __borrow_demo 10 yields buf = [0,1,...,9]. memchr for byte 4 hits at
-- offset 4 in the parent buffer (buf[4] == 4).
main : U64
main =
  case memchr (__borrow_demo 10) 4 of
    Some i -> i
    None   -> 999
