module Main
import Base

effect Tick
  tick : U64 -> U64

bad : Handler Tick U64 U64
bad = handler Tick
  once tick x k ->
    let k = \y -> y + x
    k (k 1)

useTick : () -> U64 with Tick
useTick () = Tick.tick 41

main : U64
main = handle bad in useTick ()
