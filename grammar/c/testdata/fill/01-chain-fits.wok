-- testdata/fill: the width boundary, which is the only place an off-by-one
-- in the measurement shows up. This file is CANONICAL: `wokfmt` must
-- reproduce it byte for byte.
--
-- an infix chain: the break goes before each operator, which is the
-- one token class the layout filter reads as continuing a line.
--
-- 80 columns, so it stays on one line: 80 is the last width that fits.
module M

f = aaaaaaaaaa + bbbbbbbbbb + cccccccccc + dddddddddd + eeeeeeeeee + fxxxxxxxxxx
