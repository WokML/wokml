dist-newstyle/
dist/
.ghc.environment.*
*.hi
*.o
.DS_Store
.stack-work/
cabal.project.local
.worktrees/

# BNFC generates these but we don't use them
src-generated/GeneratedParser/Wok/Test.hs
src-generated/GeneratedParser/Wok/Skel.hs
src-generated/GeneratedParser/Wok/Doc.txt

# BNFC's .bak files (backups of overwritten generated files)
*.bak

# Happy's .hs output for Par.y (cabal compiles Par.y in dist-newstyle; no need to commit)
src-generated/GeneratedParser/Wok/Par.hs
.ghc-build/

# Transient Claude harness lock files (per-plan docs/*.tasks.json stay tracked)
.claude/*.lock

# Local dev scratch swept into the branch by mistake (not slice artifacts)
.ghci
REVIEW.md

# superpowers brainstorming companion (local-only)
.superpowers/

# Blog drafts (local-only, generated notes)
blog/

CLAUDE.md
AGENTS.md

# I wonder should I do this.
CONTEXT.md

# Scratch modules the interpreter suite writes beside its fixtures; the name
# carries a hashUnique, so every run leaves a differently-named stray.
test/.interp-tmp-*.wok
# Local planning notes (readable in-repo, never committed)
plans/

# lcov/genhtml output
grammar/c/coverage/
