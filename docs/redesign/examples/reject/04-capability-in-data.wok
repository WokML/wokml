-- v2 FINAL surface REJECT. Effect type stored in a constructor. Dies at the
-- KIND fence (D4): State U64 has kind Effect, constructor fields need kind
-- Type -- before mode checking is even consulted.
module Main
import Base

effect State s
  get : s
  set : s -> ()

type Holder = Holder (State U64)
-- EXPECT: E-KIND constructor field of `Holder` has kind Effect (`State U64`);
--         fields must have kind Type
--         note: capabilities are labels, not values; declare a row entry
--         `with (c : State U64)` instead

main : U64
main = 0
