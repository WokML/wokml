module Main
import Std.Base

effect Abort = { bail : U64 -> Never }

runWith f = handle f in (let x = Abort.bail 7 in x + 1)

main : U64
main = runWith (handler Abort { once bail n k -> n ; return v -> v })
